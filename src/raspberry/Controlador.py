from utiles.getDateCurrent import getDateCurrent


def controladorIngreso():
    if(existeRegistrosFechaActual(conn,getDateCurrent )):
    
    
def controladorSalida():
    
def ingreso():
    if (buscarregistro(con)):
        if (disponibilidadBus(Fecha,conn)):
            